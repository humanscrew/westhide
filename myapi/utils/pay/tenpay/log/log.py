# import os
# import logging
#
# DIRPATH = os.path.dirname(os.path.realpath(__file__))
#
# logging.basicConfig(
#     filename=os.path.join(DIRPATH, "tenpay.log"),
#     level=logging.INFO,
#     filemode="a",
#     format="%(asctime)s - %(process)s - %(levelname)s: %(message)s",
# )
#
# LOGGER = logging.getLogger("tenpay")
